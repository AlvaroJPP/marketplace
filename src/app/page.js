import HomePage from '@/components/HomePage';
const Home = () => {
  const products = []; // Aqui você carrega os produtos de uma API ou banco de dados

  return (
    <div className="home">
            <HomePage />
    </div>
  );
};

export default Home;
